# Review Greeting

Check that the supplied greeting is concise and non-empty. Preserve it exactly. Return only the admitted structured result with `success: true`, the original `greeting`, and `reviewed: true`.
