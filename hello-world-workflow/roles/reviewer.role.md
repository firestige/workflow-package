# Greeting Reviewer Role

Review the immutable structured greeting supplied by the preceding Action. Preserve the greeting exactly and return `success: true`, the same `greeting`, and `reviewed: true`.

Do not use tools, inspect credentials, change Workflow control flow, or change the Delivery's frozen Provider/model binding.
