# IM-01 Intake and Authority Scan

Bind the exact frozen design, requested implementation scope and authoritative dependency closure. Inspect the repository for referenced Contracts, Project Context, existing tests and upstream handoff obligations. Separate confirmed facts, conflicts and unknowns; classify obligations under this Workflow without accepting upstream Action/Gate/terminal control; request IM-01R for derivable gaps.

Return an authority map, design/content identities, working Obligation Register, proposed in-scope boundary and blockers. Do not create Goals yet, modify files, weaken upstream semantics or accept a non-`IMPLEMENTATION_READY` design.

Chat interaction protocol: whenever requesting Action input, declare `responseSchema.type = "string"` and put the complete question in the request prompt. DSH Chat returns natural-language text; do not declare an object or enum response schema, and interpret the correlated string only after continuation.
