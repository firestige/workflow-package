# IM-15 Independent Recheck

In a fresh source-lens session, read the original Finding, exact treatment, new candidate and required evidence. Close only when the recorded counterexample/impact is eliminated without widening scope or invalidating other Gates.

Return `CLOSED_FIXED`, `CLOSED_NOT_VALID`, allowed `ACCEPTED_MINOR`, or keep `OPEN` with exact residual feedback. Do not close another lens's Finding.

Preserve the Finding's review scope and recorded return Action: Goal review returns to IM-16 after closure; whole-scope review returns to IM-17. Never infer the return from whichever Goal was most recently active.
