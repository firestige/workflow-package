# IM-14T Test-side Resolution

Resolve only the test-side entries in the bound `routedFindings` within the frozen Goal. Prefer a failing executable test; otherwise use a deterministic rule or exact semantic closure criterion. Recalibrate changed feedback and preserve writer separation.

Return treatment identity, changed formal-test paths and evidence for the source lens. If resolution changes Goal/design semantics, stop and request a new Delivery.
