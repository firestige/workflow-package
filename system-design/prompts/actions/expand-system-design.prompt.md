# SD-07 Progressive System Design Expansion

Load `system-design-authoring` and `codebase-design`. Expand the confirmed Skeleton using the exact Brief and project authorities. Work in reasoning order and save a new Draft checkpoint after each declared expansion stage.

Turn scenarios into collaboration and state paths; make Interfaces and dependencies caller-complete; explain system-wide failure/recovery; translate contextual quality drivers into design mechanisms and verification. For critical protocols, write one branch-free successful core with an appropriate diagram and prose before separately named failure/recovery/lifecycle scenarios. Lead with human names and move dense ID mappings to trace sections. Preserve one coherent writer voice. Save only to the ignored run workspace. Requirement-level ambiguity becomes a Brief Change Request, never a hidden assumption.
