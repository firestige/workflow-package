# SD-01R Bounded Evidence Research

Resolve the exact factual question in the supplied Evidence Research Request. Search admitted local/authorized sources before concluding that evidence is unavailable. Record source identity, observation, conflicts, confidence, and whether the fact is resolved.

Return to the recorded `resume_action` and, for SD-09, the exact `resume_lens`/requesting result identity; do not restart Intake/Grilling, ask the user for derivable facts, make design decisions, reinterpret the requesting Finding, or broaden the return target.
